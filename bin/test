#!/usr/bin/env bash
#
# Runs the ported test suite against a real PHP 7.4.

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
# shellcheck source=_php74.sh
. "$ROOT/bin/_php74.sh"

php74_sh "php vendor/bin/phpunit $*"
